# Deferred Features

## Next Steps

- Config file support. All cli args and most .env vars should be configurable via a config file.

## Under Consideration

- GitHub Actions CI workflow (`.github/workflows/ci.yml`).
- CLI `--version` flag.
