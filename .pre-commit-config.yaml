repos:
  - repo: local
    hooks:
      - id: quality-gates
        name: quality gates (format → typecheck → lint → test → coverage)
        entry: scripts/quality-gates.sh
        language: system
        stages: [pre-commit, pre-push]
        pass_filenames: false
